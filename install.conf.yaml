- clean: ['~']

- link:
    ~/.zshrc: zshrc
    ~/.oh-my-zsh: oh-my-zsh/
    ~/.vim: vim/
    ~/.gitconfig: gitconfig

- shell:
    [git submodule update --init --recursive, Installing/updating submodules]
